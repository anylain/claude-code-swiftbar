#!/bin/bash
# cc-status-writer — Claude Code hook script that writes session status to a
# small JSON file read by the SwiftBar plugin. Invoked by hooks configured in
# ~/.claude/settings.json (or project .claude/settings.json).
#
# Reads hook event JSON from stdin; writes ~/.claude/projects/<proj>/.cc-status.json
set -euo pipefail
command -v python3 >/dev/null 2>&1 || exit 0

# Save stdin to a temp file — the heredoc below will consume stdin otherwise.
tmp="$(mktemp)"
cat > "$tmp"
/usr/bin/python3 - "$tmp" "$HOME" <<'PY'
import json, os, sys, time

tmp_file = sys.argv[1]
home = sys.argv[2]
projects_dir = os.path.join(home, ".claude", "projects")

try:
    with open(tmp_file) as f:
        event = json.load(f)
except Exception:
    sys.exit(0)

event_name = event.get("hook_event_name", "")
transcript_path = event.get("transcript_path", "")
session_id = event.get("session_id", "")

if not transcript_path:
    sys.exit(0)

proj_dir = os.path.dirname(transcript_path)
status_file = os.path.join(proj_dir, ".cc-status.json")

def tool_detail(tool_name, tool_input):
    if not tool_input:
        return tool_name
    inp = tool_input if isinstance(tool_input, dict) else {}
    if tool_name == "Bash":
        return inp.get("description") or (inp.get("command") or "")[:60]
    if tool_name in ("Edit", "Write", "Read", "NotebookEdit"):
        return inp.get("file_path") or inp.get("notebook_path") or tool_name
    if tool_name == "TaskCreate":
        return inp.get("subject") or tool_name
    if tool_name in ("Grep", "Glob"):
        return inp.get("pattern") or tool_name
    return (inp.get("description") or inp.get("query") or tool_name)[:60]

def write_status(state, detail=""):
    ts = int(time.time())
    payload = json.dumps({"state": state, "detail": detail, "ts": ts, "sid": session_id})
    try:
        with open(status_file, "w") as f:
            f.write(payload)
    except Exception:
        pass

def clear_status():
    try:
        os.remove(status_file)
    except FileNotFoundError:
        pass
    except Exception:
        pass

if event_name == "SessionEnd":
    clear_status()

elif event_name == "SessionStart":
    write_status("running", "starting session…")

elif event_name == "UserPromptSubmit":
    write_status("running", "Claude is responding…")

elif event_name == "PreToolUse":
    name = event.get("tool_name", "?")
    detail = tool_detail(name, event.get("tool_input", {}))
    write_status("running", f"using {name}: {detail}")

elif event_name == "PostToolUse":
    name = event.get("tool_name", "?")
    write_status("running", f"finished {name}")

elif event_name == "PostToolBatch":
    write_status("running", "processing results…")

elif event_name == "PermissionRequest":
    name = event.get("tool_name", "?")
    detail = tool_detail(name, event.get("tool_input", {}))
    write_status("needs-permission", f"approve {name}: {detail}")

elif event_name == "PermissionDenied":
    write_status("running", "permission denied, continuing…")

elif event_name == "Stop":
    stop_reason = event.get("stop_reason", "")
    tool_calls = event.get("tool_calls", []) or []
    if stop_reason == "end_turn" and not tool_calls:
        write_status("needs-input", "awaiting your input")
    elif stop_reason == "max_tokens" and not tool_calls:
        write_status("error", "output truncated (max_tokens)")
    else:
        if tool_calls:
            tc_names = [tc.get("name", "?") for tc in tool_calls[:3]]
            write_status("running", f"executing {', '.join(tc_names)}…")
        else:
            write_status("running", "Claude is responding…")

elif event_name == "StopFailure":
    err_type = event.get("error_type", "")
    err_msg = (event.get("error_message", "") or "")[:80]
    write_status("error", f"{err_type}: {err_msg}" if err_type else err_msg or "unknown error")

elif event_name == "PreCompact":
    write_status("running", "compacting context…")

elif event_name == "PostCompact":
    write_status("running", "processing after compaction…")

# Other events are intentionally ignored.
PY
rm -f "$tmp"

# ── Trailing-edge debounce: collapse hook bursts into one URL refresh ──────
# Multiple hooks within 1s share a single trailing worker; only the worker
# that wins the LOCK race fires `swiftbar://refreshplugin?name=claude-code`.
# Spec: docs/superpowers/specs/2026-06-14-swiftbar-cpu-perf-design.md
PENDING="/tmp/cc-swiftbar-pending-$UID"
LOCK="/tmp/cc-swiftbar-lock-$UID"

date +%s.%N > "$PENDING" 2>/dev/null || true

if [ -e "$LOCK" ]; then
  exit 0
fi

(
  : > "$LOCK"
  trap 'rm -f "$LOCK"' EXIT
  sleep 1
  rm -f "$PENDING"
  /usr/bin/open -g "swiftbar://refreshplugin?name=claude-code" 2>/dev/null || true
) </dev/null >/dev/null 2>&1 &
